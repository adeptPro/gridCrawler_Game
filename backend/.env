# MongoDB Connection URI
# Replace with your actual MongoDB connection string.
# If using Docker Compose with a 'mongo' service, this might be:
# MONGO_URI=mongodb://mongo:27017/aframeGameDB
# For a local MongoDB instance not in Docker, it might be:
# MONGO_URI=mongodb://localhost:27017/aframeGameDB
# For MongoDB Atlas, use the connection string they provide.
MONGO_URI=mongodb://mongo:27017/aframeGameDB

# Server Port
PORT=5001 